services:
  validation-service:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: file-validation-service
    volumes:
      - ./src/files:/app/src/files
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m src.validation_service.run_valid_to_check raw

  validation-service-source:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: file-validation-source
    volumes:
      - ./src/files:/app/src/files
      - ./.env:/app/.env
    environment:
      - PYTHONUNBUFFERED=1
    command: python -m src.validation_service.run_valid_to_check source
    depends_on:
      - validation-service